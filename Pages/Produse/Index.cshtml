@page
@model Site_Baras_Andrei.Pages.Produse.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Produs[0].DenumireProdus)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Produs[0].Ingredient.NumeIngredient)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Produs[0].ValoareNutritionala.Kcal)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Produs) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DenumireProdus)
            </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Ingredient.NumeIngredient)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.ValoareNutritionala.Kcal)
                </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
